def catch_where(bot, message):
    bot.send_message(message.chat.id, 'in Lviv')

def catch_math(bot, message):
    bot.send_message(message.chat.id, '(841*25/2*7690412+10000)=???')

def catch_kiss(bot, message):
    bot.send_message(message.chat.id, '*чмок*')

def catch_weather(bot, message):
    bot.send_message(message.chat.id, 'Зашибумба а не погода')

def catch_tooth(bot, message):
    bot.send_message(message.chat.id, 'ай вуб бовить')

